// No helpers needed; components operate on IIssue directly
// Minimal shape adapter for ReadButton

// Minimal shape adapter for ReadButton
// No helpers needed; components operate on IIssue directly
